export enum ButtonTypeEnum {
  Link = 'Link',
  button = 'button',
  a = 'a',
  ref = 'ref',
}

export enum SigningInStatusEnum {
  SigningIn = 'SigningIn',
  SigningUp = 'SigningUp',
}

export enum StandardButtonButtonTypeEnum {
  submit = 'submit',
  button = 'button',
}

export enum AccessLevelEnum {
  NotSignedIn = 'not-signed-in',
  Basic = 'basic-plan',
  Subscriber = 'subscriber',
  SuperAdmin = 'super-admin',
}

export enum DictionaryType {
  Words = 0,
  Phrases = 1,
  Both = 2,
}
