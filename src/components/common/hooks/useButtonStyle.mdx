import { Meta } from '@storybook/blocks';

import * as useButtonStyleStories from './useButtonStyle.stories';

<Meta of={useButtonStyleStories} />

# useButtonStyle

useButtonStyle is a hook that returns a button style object. It takes two optional arguments, which are `type` and `style`.

## Usage

```import { useButtonStyle } from '@storybook/components';`
