export function populateMatchesElements(matches: any, element: any) {
  console.log(matches);
  // matches.forEach((match: any) => {
  //   let node = document.createElement('li')
  //   let text = document.createTextNode(match['word'] + " - " + match['definition'])
  //   node.appendChild(text)
  //   element.appendChild(node)
  // })
}
